# Render: Root Directory = ``backend``. $PORT is set by Render — always pass --bind.
# ``main:app`` → ``backend/main.py`` (shim to ``fastapi_service.main:app``).
web: gunicorn main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --workers 2 --timeout 120
